<%= form_with(model: mission, local: true) do |f| %>
  <%= f.text_area :content %>
  <%= f.datetime_select :deadline %>

    <%= f.select :status, [['未完了',0],['完了',1]] %>

  <%= f.submit 'カウントダウン開始' %>
<% end %>