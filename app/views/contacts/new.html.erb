<% provide(:title, "お問い合わせ") %>
<div class="container">
  <div class="row py-5 d-flex flex-column">
    <div class="col-11 col-md-8 col-lg-6 mx-auto mb-3 p-3 p-md-4 border border rounded-lg shadow bg-white">
      <div class="col text-center text-info"><h3>お問い合わせ</h3></div>
      <div class="col form-group pt-3">
        <%= form_with model: @contact, url: contact_confirm_path, local: true do |f| %>
          <div class="form-group">
            <%= f.text_field :name, placeholder: 'お名前', class: 'col-10 col-md-7 mt-4 mx-auto form-control shadow-sm rounded-pill bg-white', required: :required %>
          </div>
          <div class="form-group">
            <%= f.text_field :email, placeholder: 'メールアドレス', class: 'col-10 col-md-7 mt-4 mx-auto form-control shadow-sm rounded-pill bg-white', required: :required %>
          </div>
          <div class="form-group">
            <%= f.text_area :message, rows: 5, placeholder: 'お問い合わせ内容', class: 'col-10 col-md-7 mt-4 mx-auto form-control shadow-sm rounded bg-white', required: :required %>
          </div>
          <div class="text-center">
            <%= f.submit '入力内容確認' ,class: 'btn btn-outline-info shadow-sm rounded-pill mt-3'%>
          </div>
        <% end %>
      </div>
    </div>
    <div class="col-8 mt-4 mx-auto text-center">
      <p>回答するまでに数日お時間をいただく場合がございます</p>
      <p>あらかじめご了承ください</p>
    </div>
  </div>
</div>